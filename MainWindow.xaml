<Window x:Name="WorkspaceManagerMainWindow" x:Class="WindowsWorkspaceManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WindowsWorkspaceManager"
        mc:Ignorable="d"
        Title="Workspace Manager" Height="483" Width="421" ResizeMode="NoResize">
    <Grid Margin="10,10,10,10">
        <Button x:Name="Button_TemplateFolderAdd" Content="Add" Margin="178,253,0,0" VerticalAlignment="Top" Width="82" HorizontalAlignment="Left" Command="{Binding AddCommand}"/>
        <DataGrid x:Name="DataGrid_RegisteredTemplateFolder" ItemsSource="{Binding FilteredTemplates}" SelectedItem="{Binding SelectedTemplate}" Margin="15,44,0,0" HorizontalAlignment="Left" Width="359" Height="192" VerticalAlignment="Top" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Extended">
            <DataGrid.Columns>
                <DataGridTextColumn Header="No" Binding="{Binding No}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Templates" Binding="{Binding TemplateName}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Last Date" Binding="{Binding LastCreatedDate}" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="Button_TemplateFolderRemove" Content="Remove" Margin="292,253,0,0" VerticalAlignment="Top" Width="82" HorizontalAlignment="Left" Command="{Binding RemoveCommand}" CommandParameter="{Binding SelectedItems, ElementName=DataGrid_RegisteredTemplateFolder}"/>
        <TextBox x:Name="TextBox_TargetFolderToCreate" HorizontalAlignment="Left" Margin="15,324,0,0" VerticalAlignment="Top" Width="254" Text="{Binding TargetFolder, UpdateSourceTrigger=PropertyChanged}"/>
        <Button x:Name="Button_TemplateFolderCreate" Content="Create" Margin="292,389,0,0" VerticalAlignment="Top" Width="82" HorizontalAlignment="Left" Command="{Binding CreateCommand}"/>
        <TextBox x:Name="TextBox_WorkspaceName" HorizontalAlignment="Left" Margin="15,355,0,0" VerticalAlignment="Top" Width="254" Text="{Binding WorkspaceName, UpdateSourceTrigger=PropertyChanged}"/>
        <CheckBox Content="Date (yyyymmdd)" HorizontalAlignment="Left" Margin="15,295,0,0" VerticalAlignment="Top" Width="127" IsChecked="{Binding IsDateChecked}"/>
        <CheckBox Content="Time (hhmmss)" HorizontalAlignment="Left" Margin="147,295,0,0" VerticalAlignment="Top" Width="122" IsChecked="{Binding IsTimeChecked}"/>
        <TextBox x:Name="TextBox_SearchTemplate" HorizontalAlignment="Left" Margin="65,11,0,0" VerticalAlignment="Top" Width="309" Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"/>
        <Button x:Name="Button_SelectDestinationFolder" Content="Select" Margin="292,323,0,0" VerticalAlignment="Top" Width="82" HorizontalAlignment="Left" Command="{Binding SelectFolderCommand}"/>
        <Label x:Name="Label_Search" Content="Search" HorizontalAlignment="Left" Margin="15,7,0,0" VerticalAlignment="Top"/>
        <RadioButton Content="RadioButton" HorizontalAlignment="Left" Margin="775,207,0,0" VerticalAlignment="Top"/>

    </Grid>
</Window>
